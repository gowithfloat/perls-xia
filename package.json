{
  "name": "xia-perls",
  "version": "1.0.0",
  "description": "Experience Agent for PERLS",
  "main": "build/index.js",
  "scripts": {
    "start": "NODE_PATH=./build node build/index.js",
    "build": "tsc -p .",
    "debug_docker": "docker compose -f docker-compose.debug.yml up -d --build",
    "watch": "cp -R ./src/schema ./build/ && tsc -w -p .",
    "debug": "nodemon --watch ./build --inspect-brk=0.0.0.0:9229 --nolazy ./build/index.js"
  },
  "keywords": [
    "xia",
    "perls",
    "agent",
    "indexing",
    "experience"
  ],
  "author": "Float LLC",
  "license": "MIT",
  "devDependencies": {
    "@types/jsonapi-serializer": "^3.6.5",
    "@types/node": "^16.10.3",
    "nodemon": "^2.0.13",
    "ts-node": "^10.2.1",
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "axios": "^0.22.0",
    "form-data": "^4.0.0",
    "jsonschema": "^1.4.0"
  }
}
